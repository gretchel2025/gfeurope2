# Local app URL used for QR-code links during development
APP_BASE_URL=http://localhost:5173

# Default event id used for root redirects and bootstrap/setup defaults
APP_EVENT_ID=gfeu2026

# Enable Supabase email/password login on hosted deployments when explicitly needed.
# Local development still has its own local-only admin/password path below.
ENABLE_EMAIL_PASSWORD_AUTH=false

# Supabase Auth and app data. For local Supabase CLI defaults, use:
# PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
# PUBLIC_SUPABASE_PUBLISHABLE_KEY=<local anon/publishable key from `supabase status`>
# SUPABASE_SERVICE_ROLE_KEY=<local service_role key from `supabase status`>
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Initial ticket counters for a brand-new local database
STANDARD_TICKETS_INITIAL_AVAILABLE=200
GRAND_FEAST_PLUS_TICKETS_INITIAL_AVAILABLE=50

# Optional if you want email sending to work locally
RESEND_API_KEY=
EMAIL_FROM="Grand Feast EU and UK <help@grandfeast.eu>"
EMAIL_REPLY_TO="Grand Feast EU and UK <help@grandfeast.eu>"

# Required for hosted booking payment-proof uploads.
# Local dev falls back to inline proof storage when these values are empty.
CLOUDINARY_URL=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Optional if you want local Supabase Google OAuth to work.
# Google Cloud OAuth must include this redirect URI:
# http://127.0.0.1:54321/auth/v1/callback
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=

# Optional offline-friendly local Supabase email/password auth.
# Used only by `make setup-local-auth` and the local-only sign-in form.
LOCAL_DEV_AUTH_EMAIL=admin@example.test
LOCAL_DEV_AUTH_PASSWORD=password
LOCAL_DEV_AUTH_ROLES=tester,admin,superuser
LOCAL_DEV_AUTH_EVENT_ROLES=gfeu2026:admin

# Optional local-only secrets for Codex/test service accounts on live-dev.
# Do not add these to Netlify. Store real values only in your untracked .env.
LIVE_DEV_CODEX_TESTER_EMAIL=codex-tester@grandfeast.eu
LIVE_DEV_CODEX_TESTER_PASSWORD=
LIVE_DEV_CODEX_ADMIN_EMAIL=codex-admin@grandfeast.eu
LIVE_DEV_CODEX_ADMIN_PASSWORD=
LIVE_DEV_SUPABASE_URL=https://guoqhigzyfisvtnlrbjw.supabase.co
LIVE_DEV_SUPABASE_SERVICE_ROLE_KEY=

# Optional local-only secrets for the Codex production superuser service account.
# Do not add these to Netlify. Store real values only in your untracked .env.
PROD_CODEX_SUPERUSER_EMAIL=codex-prod@grandfeast.eu
PROD_CODEX_SUPERUSER_PASSWORD=
PROD_SUPABASE_URL=https://erhrykkyhsygnonyfbis.supabase.co
PROD_SUPABASE_SERVICE_ROLE_KEY=

# Optional Playwright e2e overrides. Hosted e2e falls back to the Codex service-account
# variables above when E2E_ADMIN_* is not set.
E2E_ENV=local
E2E_BASE_URL=
E2E_EVENT_ID=gfeu2026
E2E_ADMIN_EMAIL=
E2E_ADMIN_PASSWORD=
E2E_BOOKING_EMAIL=
E2E_ALLOW_PROD_MUTATION=false
